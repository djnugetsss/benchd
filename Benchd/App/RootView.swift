import SwiftUI

/// The app's root. For now it shows `PlaceholderScreen` — v1 navigation
/// (Profile / Wraps / Players / Settings) replaces this once those features exist.
struct RootView: View {
    var body: some View {
        PlaceholderScreen()
    }
}

#Preview {
    RootView()
}
