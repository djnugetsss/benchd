# Sleeper

Read-only client for the public Sleeper API (no auth required).
Used to build the v1 profile: leagues, rosters, matchups, drafts.

Views never call this directly — a feature view model does.
